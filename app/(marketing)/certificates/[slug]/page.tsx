import { certificates } from '@/data/certificates'; import { notFound } from 'next/navigation'; import { Card, PageHeader, Badge } from '@/components/ui/primitives';
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const c=certificates.find(x=>x.slug===slug); if(!c) return notFound(); return <main className='mx-auto max-w-4xl p-6'><PageHeader title={c.title} subtitle={c.shortDescription}/><div className='grid gap-4 md:grid-cols-3'><Card><p>Status</p><Badge>{c.status}</Badge></Card><Card><p>Abschnitte</p><p>{c.sectionCount}</p></Card><Card><p>Mock Exams</p><p>{c.mockExamCount}</p></Card></div></main>}
