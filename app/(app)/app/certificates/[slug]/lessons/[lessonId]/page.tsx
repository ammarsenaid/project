import { PageHeader, Card, Button } from '@/components/ui/primitives'; export default async function Page({params}:{params:Promise<{slug:string;lessonId:string}>}){const {slug,lessonId}=await params; return <div><PageHeader title={`Lesson ${lessonId}`} subtitle={`German canonical content · ${slug}`}/><Card className='space-y-2'><p>German exam wording panel</p><p>Assistive language toggle: DE/AR/EN/TR</p><Button variant='secondary'>Mark complete</Button></Card></div>}
